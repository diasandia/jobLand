$('.show-user-note').prepend('<%= j render partial: "notes/show_note", locals: { note: @note } %>');

$('#note_description').val("");

$('#note-link').click(function(e){
  e.preventDefault();
  $(".show-user-note").toggle();
});

